# 来自 Claude 的建议

> 基于对 236+ 篇论文笔记、4 本 mdbook 知识库、个人观点文章的深度分析，结合研究方向讨论整理而成。2026.06.08

---

## 一、现状评估

### 你的位置

- 知识覆盖面极广：从传统物理仿真 (SPH/MPM/XPBD/IPC) 到 RL 角色控制 (DeepMimic/AMP/ASE) 到生成式动画 (Diffusion T2M/3DGS Animation) 均有系统性积累
- 最突出的能力是**分类学思维**——能把散落的论文组织成结构化技术地图（RL 五分类、混合方法四套路、Diffusion T2M 按问题维度分类）
- 博客文章 (Views) 展示了诚实的工业判断力——"动画师不倾向于使用这种技术"、"质量上与动画师有较大差距"

### 知识深度分布

| 层次 | 占比 | 代表 |
|------|------|------|
| 深入理解，有原创洞见 | ~35% | AMP, DeepMimic, TaoAvatar, iLQR, IPC/XPBD |
| 忠实复述，有补充直觉 | ~30% | DDPM, Flow Matching, 部分 GAMES103 |
| 有分析，有定位 | ~20% | PDP, AnimateAnyMesh, 3DGS 分类 |
| 表面摘要 | ~15% | PhysDiff, MotionGPT, 早期视频扩散论文 |

### 核心矛盾

**最大风险是永远在读、永远在整理、永远不产出。** 236 篇论文笔记是惊人的数字，但知识的广度已经够了。缺的不是知识宽度，而是对某个具体系统的深度理解——这种理解只能通过动手获得。

---

## 二、知识盲区

以下领域对研究方向至关重要，但目前几乎没有覆盖：

1. **可微仿真 (Differentiable Simulation)**：Brax、MuJoCo-XLA、DiffTaichi、Warp。连接物理仿真与神经网络的工程基础，做 physics-aware generation 绕不过去的基础设施
2. **评估方法论 (Evaluation Metrics)**：FID/diversity/physical plausibility 这些指标本身的局限性。当前领域的一个真正痛点——大家不知道什么算"好"的物理合理动作
3. **World Models for Physics**：Genie、UniSim、GameGen-X、DIAMOND。与"从视频学习物理动态"的兴趣直接相关，是 2024-2026 最热的方向之一
4. **生物力学/肌肉仿真**：肌肉驱动模型限制了生物合理性的理解
5. **多角色交互**：所有已读方法都是单人的，真实场景需要多人交互
6. **Sim-to-Real Transfer**：RL 知识线与机器人迁移直接相关，但笔记中几乎没有覆盖

---

## 三、方向建议

### 不要做的事

- **不要做视频生成**：T2V/视频编辑被大厂主导 (Sora, Kling, Vidu, HailuoAI...)，核心壁垒是算力和数据规模。把视频生成纯粹当作"应用场景"——生成的物理合理动作可以作为视频生成的条件输入，但不要去卷模型架构
- **不要继续无差别地读论文**：从今天起，只读与核心问题直接相关的论文，其余最多存个标题

### 核心方向：物理感知的动作生成与控制

> **"如何让生成式运动模型产出物理上合理的动作？"**

这个问题正好在最强的两个领域（物理仿真 × 生成模型）的交叉处。PhysDiff、PDP、CLOSD、UniPhys、Maskedmimic 都已深入读过，Hybrid 方法笔记中已提炼出四种结合模式。比大多数人更适合做这个方向。

### 三个热点的真实对应关系

```
已有积累                           热点领域
─────────────────────────────────────────────────────────
DeepMimic/AMP/ASE/Maskedmimic  →  具身智能 (人形机器人控制)
物理仿真 (MPM/XPBD/IPC)        →  物理AI / 可微仿真
Diffusion T2M / Flow Matching  →  世界模型 (动作生成作为先验)
3DGS Animation / TaoAvatar     →  数字人 / 仿真资产生成
```

#### 具身智能：最直接的机会

当前具身智能领域有结构性矛盾：做机器人的人不懂动画，做动画的人不碰机器人。人形机器人的核心难题——全身运动控制、步态、平衡、摔倒恢复——本质上就是 DeepMimic/AMP 那条线。

| 公司/项目 | 技术 | 关联 |
|-----------|------|------|
| Unitree G1/H1 | RL (Isaac Gym 训练) | DeepMimic 系 |
| Figure 02 | RL + 全身控制 | iLQR/MPC 知识 |
| Tesla Optimus | 端到端学习 | Hybrid 方法范式 |
| Berkeley H1 灵巧操作 | 分层 RL + diffusion prior | Maskedmimic + Diffusion T2M |
| NVIDIA GR00T N1 | foundation model for humanoid | 所有知识的交汇点 |

**优势**：大多数具身智能研究者来自机器人学或 DL，对物理仿真角色动画的理解很浅。他们不知道 AMP 为什么有效、phase conditioning 为什么重要、motion prior 的质量如何影响控制策略。

#### 世界模型：动作层面的世界模型

不需要和 Sora/Genie/DIAMOND 竞争通用世界模型。可以做世界模型中被严重低估的组件——给定指令（文本/目标位置），预测物理合理的动作序列。目前大多数 motion generation 模型不关心物理合理性（脚穿模、质心漂浮），少数加了物理约束的（PhysDiff、PDP）效果有限。机器人领域需要的不是"好看的动作"，而是"能真的执行的动作"。

#### 物理 AI：物理仿真功底的用武之地

NVIDIA Cosmos 是这个方向的旗舰。大多数做物理 AI 的人只会调用仿真器，不理解仿真算法本身。

- 可微仿真（接触问题、sim-to-real gap 的主要来源）
- 用物理仿真知识诊断和改进现有物理 AI 系统

### 一条整合路径

与其三选一，找一条线把三个热点串起来：

```
Motion Prior (Diffusion)     ← 世界模型层面
        ↓
Physics-Aware Loss           ← 物理AI层面
        ↓
RL Control (Isaac Gym)       ← 具身智能层面
```

**具体目标**：训练一个物理约束的动作扩散模型，然后把它用作 Isaac Gym 中人形机器人 RL 控制的 motion prior。

这个项目：
- 用到最擅长的三条知识线（Diffusion T2M + 物理仿真 + RL 控制）
- 同时贴近三个热点
- 单卡可以跑（Isaac Gym + 中等规模 diffusion model）
- 有明确的改进空间（物理约束怎么加、prior 质量怎么评估）
- 和 NVIDIA GR00T、Unitree、Figure 做的事情方向一致

---

## 四、执行路径：从"读论文"到"做研究"

核心问题不是"选方向"，而是**从"读论文"过渡到"做研究"**。这两者之间的鸿沟不是靠多读几篇论文能填上的，必须靠动手。

### 原则：复现 → 改动 → 发现

不要试图一开始就"提出一个新方法"。先挑一篇读得最深的论文跑通，然后改一个小东西看看会怎样。这个过程会自然地告诉你下一步该做什么。

### 起点：Maskedmimic (笔记编号 183)

选它的理由：
- **开源**：GitHub 上有代码，基于 Isaac Gym
- **已有深入理解**：Hybrid 方法笔记里把它定位为"通过 masked motion 实现统一物理控制"
- **单 GPU 能跑**：Isaac Gym 是 GPU 并行仿真
- **改动空间明确**：mask 设计、motion prior 训练、奖励函数都可以调

### 具体步骤

**第 1 步（1-2 天）：搭环境**

安装 Isaac Gym/Isaac Lab → 克隆代码 → 跑通预训练模型的推理

目标：看到一个角色在屏幕上做出物理合理的动作。这是"hello world"。

**第 2 步（3-5 天）：复现训练**

用论文给的超参数从头训练 → 看 loss 曲线是否和论文一致 → 评估指标是否能复现

目标：理解训练过程中的每一个工程细节。遇到的 100 个论文里没写的问题，**就是研究的起点**。

**第 3 步（1-2 周）：改一个小东西**

| 改动 | 学到的东西 | 难度 |
|------|-----------|------|
| mask 比例从论文设定改成别的值 | 理解 mask 对训练稳定性的影响 | ⭐ |
| reward 里加物理约束（如质心动量惩罚） | 理解"物理引导"到底有多大用 | ⭐⭐ |
| motion prior 从 VAE 换成 diffusion model | 理解 prior 质量对最终控制的影响 | ⭐⭐⭐ |
| 加新的 mask 类型（如只给手部 trajectory） | 探索细粒度控制的边界 | ⭐⭐ |

**第 4 步：记录发现**

不管改动结果是正面还是负面，记录下来：改了什么、为什么改、结果如何、解释是什么。**这就是研究的本质——不是提出多炫酷的方法，而是理解因果关系。**

### 如果 Maskedmimic 跑不通

备选起点（同样开源、单卡可跑）：

| 论文 | 代码 | 学到什么 |
|------|------|---------|
| MDM (Motion Diffusion Model) | github.com/GuyTev/motion-diffusion-model | Diffusion 运动生成完整 pipeline，纯 PyTorch，不需要仿真器 |
| T2M-GPT | github.com/Mael-zys/T2M-GPT | 离散化运动生成，理解 VQ-VAE 在动作上的实际表现 |
| PhysDiff | github.com/nv-tlabs/PhysDiff | 物理约束 + diffusion 的最直接结合 |

MDM 和 T2M-GPT 不需要物理仿真器，纯 PyTorch，GPU 同步问题更小。但**迟早要回到仿真器**，因为最终需要物理合理性。

### 在做实验的过程中确定方向

兴趣不是想出来的，是做出来的。在跑实验的过程中：

- 如果对**仿真器的物理建模**更感兴趣 → 方向偏物理仿真
- 如果对 **motion prior 的质量**更感兴趣 → 方向偏生成模型
- 如果对**控制策略的泛化**更感兴趣 → 方向偏 RL
- 如果发现 Isaac Gym 的限制太大 → 去研究 differentiable simulation (Brax/MuJoCo-XLA)

---

## 五、长期趋势判断 (2026-2028)

"动画与仿真"领域最重要的三个趋势：

1. **物理基础模型 (Physics Foundation Model)**：类似 LLM 之于语言，一个大规模的、预训练的、能理解和生成物理合理运动的通用模型。目前还没有人真正做出来
2. **Sim-to-Real for Humanoids**：随着人形机器人硬件爆发 (Unitree, Figure, Tesla Optimus)，对物理仿真中训练的策略迁移到真实机器人的需求会暴增
3. **交互式多角色动画**：几乎所有运动生成论文都是单人的。真实场景需要多人交互（舞蹈、格斗、日常社交）。技术难度高但几乎没有竞争

---

## 六、关于产出形式

目标不是出论文，而是让自己更专业。但有成果当然更好。适合的产出形式：

- **实验记录/技术博客**：把复现过程和改动发现写下来，比论文笔记更有价值
- **批判性观点文章**：类似 Views 系列的博客，诚实的 industry-grounded 判断在当前充满 hype 的学术圈非常难得
- **开源工具/实验代码**：一个小而精的实验框架比 236 篇笔记更能建立专业声誉

---

## 核心原则

> **选一个问题，搭一个实验环境，做一个小实验，写一个结论。**
>
> 236 篇笔记证明有做一流研究的认知基础，但研究的影响力来自产出，不来自输入。

> **完成一个闭环比"知道很多方向"更能让你成为专业研究员，也更能创造机会。**
>
> 一个做过完整实验的人，和一个读过 236 篇论文的人，在面试/合作/讨论中的表现完全不同。前者能回答"你试过什么、什么行什么不行、为什么"，后者只能回答"某篇论文说了什么"。

> **兴趣不是想出来的，是做出来的。**

---

## 七、双方向并行：主观角色 + 客观世界（2026.06.15 补充）

### 两个方向

```
方向 A：主观角色的物理（机器人）
  入口：Maskedmimic / Isaac Lab
  工具：Isaac Gym + Diffusion Model
  闭环标志：一个物理约束的 motion prior 控制了机器人
  特点：有成熟开源代码，路径清晰

方向 B：客观世界的物理（AI + 物理仿真）
  入口：GS 物理仿真 / Mesh + AI 修正
  工具：自己的仿真代码 + 神经网络
  闭环标志：一个可验证的物理仿真改进
  特点：benchmark 不成熟，探索性更强
```

### 同时做的合理性

- 两个方向的知识你已经都有（MPM/XPBD/IPC + Diffusion/RL）
- 两个方向可以互相反馈：世界越真 → 角色训练越有效；角色越懂物理 → 对世界物理的要求越明确
- 懂世界物理的人 + 懂角色物理的人 = 很少有人同时具备

### 同时做的风险

最可能的失败模式：两个方向各做了一点 → 都没有闭环 → 觉得自己进展慢 → 回去读更多论文 → 又回到"永远不产出"的状态。

### 并行结构：交替聚焦

不是两个方向同时全速推进，而是交替聚焦，每个方向有明确的小目标：

```
第 1~2 周：集中做方向 A
  目标：跑通 Maskedmimic，完成一个小改动
  这段时间方向 B 只读相关论文，不写代码

第 3~4 周：集中做方向 B
  目标：跑通 GS 物理仿真的一个 baseline
  这段时间方向 A 只记录想法，不写代码

每两周切换一次，每次切换前写下：
  这次做了什么
  下次从哪继续
```

### 两个方向各自的第一个闭环

**方向 A 的第一个闭环：**

```
Maskedmimic 跑通训练
→ 改一个东西（比如 reward 加物理约束）
→ 对比改前改后的结果
→ 写一个记录
```

**方向 B 的第一个闭环：**

```
现有的 GS/Mesh 物理仿真跑通
→ 加一个 AI 组件（比如学残差修正，或学接触力预测）
→ 和纯传统方法对比
→ 写一个记录
```

### 判断标准

```
问自己：过去两周我有没有产出？

产出 = 跑了一个实验 + 得到一个结论 + 写了下来

如果两个方向都没有产出 → 节奏有问题，收缩到一个方向
如果有一个方向有产出   → 继续，另一个方向暂时降低优先级
如果两个方向都有产出   → 在做对的事
```

### 两条线最终怎么串起来

```
短期（现在）：
  两个方向各自独立推进，交替聚焦

中期（半年后）：
  两个方向都有初步闭环 → 让角色在 GS 物理世界里行动
  → 这就变成了别人做不了的事情

长期：
  GS 原生物理 + 物理感知角色控制
  在 GS 表达的世界里，用 AI 弥补物理局限
  同时解决客观世界和主观角色两条线的物理问题
```

### 原则

两个方向可以同时存在，但不要同时在两个方向上各开多个实验。每个方向在同一时间只有一个实验在跑。

---

## 八、方向调整与重新规划（2026.06.27 补充）

### 进展回顾

截至 2026.06.27，实际进展：

**方向 A（Isaac Lab）：**
- Isaac Lab 已成功安装并跑通
- 选择 Isaac Lab（Isaac Gym 的继任者，官方主推）
- **没有运行 Maskedmimic**

**方向 B（物理仿真）：**
- 在做刚软交互物理仿真（XPBD + Euler 积分）
- 解决了多个具体问题（坐标系旋转、穿模、抖动、碰撞响应）
- 有 GPU 计算着色器优化经验
- 有实质产出（物理仿真.md、ComputerShader性能优化.md）

### Maskedmimic 不适合的原因

经过实践和思考，Maskedmimic 不是适合的方向：

1. **资源和数据问题**：需要大量运动捕捉数据和 GPU 资源，这是现实约束
2. **技术本质问题**：Maskedmimic 的核心是 diffusion model，只是生成空间从动作变成 PD 参数，仍然是基于大量数据的生成模型，与机器人控制关系不大
3. **与兴趣不匹配**：更关心物理仿真本身，而不是基于数据的生成

### 重新定义方向

基于实际情况和兴趣，重新定义两个方向：

**方向 A：具身智能**
- 工具：Isaac Lab
- 目标：理解 RL 控制流程，训练机器人完成物理任务
- 闭环标志：一个机器人学会了一项物理任务（行走、抓取等）
- 现状：Isaac Lab 已跑通，有系统的 RL 理论基础（见 GAMES105_mdbook/src/ReinforcementLearning.md）

**方向 B：物理仿真**
- 工具：自己的物理引擎（非 Isaac Sim）
- 目标：改进物理仿真的准确性和效率
- 闭环标志：一个可验证的物理仿真改进
- 现状：刚软交互系统已有实质进展

**重要说明**：物理仿真系统属于另一个物理引擎，不能与 Isaac Sim 结合，但经验可以迁移，帮助理解 Isaac Sim 和其他物理仿真系统。

### 核心竞争力：物理感知的机器人控制

基于背景和目标，最适合的技术专家方向：

**"物理感知的机器人控制"（Physics-Aware Robot Control）**

核心能力：
1. **物理仿真理解**：已有（刚软交互、碰撞响应、GPU 优化）
2. **RL 控制**：有理论基础，需要补充实践（Isaac Lab）
3. **Sim-to-Real Transfer**：需要理解和实践

稀缺性分析：
- 做物理仿真的人不懂 RL，做 RL 的人不懂物理仿真
- 能够 bridging the gap 的人非常稀缺
- 人形机器人公司都在找这样的人

### 具体路径规划

#### 阶段 1：补齐 RL 实践（1-2 个月）

**目标**：从理论到实践，理解 RL 控制的完整流程

**具体任务**：
1. 用 Isaac Lab 跑通一个基础的 RL 控制任务（人形机器人行走）
2. 尝试不同的 RL 算法（PPO 等）
3. 理解奖励设计（参考 DeepMimic, AMP 等方法）

**产出**：
- 学习记录：Isaac Lab 的 RL 控制流程
- 实验记录：不同算法/奖励设计的效果对比
- 一个可工作的机器人控制 demo

#### 阶段 2：深入物理仿真（1-2 个月）

**目标**：在物理仿真领域建立深度

**具体任务**：
1. 继续优化当前的刚软交互系统（稳定性、碰撞响应、性能）
2. 选择一个深入方向（三选一）：
   - 可微分物理仿真：让物理仿真可微分，与神经网络结合
   - 接触问题：改进接触力计算、摩擦模型
   - 效率优化：GPU 并行化、算法优化
3. 建立 baseline 和对比（传统方法 vs 改进方法）

**产出**：
- 技术博客：关键问题和解决方案
- 实验记录：改进前后的对比
- 开源代码（如果适合开源）

#### 阶段 3：结合点（2-3 个月后）

**目标**：把物理仿真和 RL 控制结合起来

**具体任务**：
1. 研究物理仿真准确性对 RL 控制的影响（调整物理参数，观察训练效果）
2. Sim-to-Real Transfer（理解 sim-to-real gap，实践域随机化）
3. 发表技术文章或博客，建立专业声誉

**产出**：
- 技术报告/博客：物理仿真对 RL 控制的影响
- 实验数据：定量分析
- 如果有突破，可以考虑论文

### 关于职业竞争力的具体建议

#### 短期（6 个月内）

**建立可见的技术产出**：
1. 技术博客系列："物理仿真 × 具身智能"系列
2. 开源项目：物理仿真模块或 Isaac Lab 的 RL 控制示例
3. 社区参与：Isaac Lab 社区、物理仿真开源项目

#### 中期（6-12 个月）

**建立专业声誉**：
1. 技术演讲：参加技术会议（GAMES、SIGGRAPH 等）
2. 合作项目：与机器人实验室合作
3. 深度技术文章：建立"物理仿真 × 具身智能"专家的形象

#### 长期（1-2 年）

**成为技术专家**：
1. 在某个具体方向上有深入研究（可微分物理仿真、接触问题、sim-to-real transfer）
2. 有可验证的成果（开源项目被广泛使用、技术文章被引用）
3. 有明确的职业定位（人形机器人公司技术专家、物理仿真资深工程师、具身智能研究员）

### 关于资源限制的应对

缺少资源（数据和 GPU）是现实约束，应对策略：

1. **不做需要大量数据的方向**：放弃世界模型、大规模运动生成，聚焦物理仿真 × RL 控制
2. **充分利用现有资源**：Isaac Lab（GPU 并行仿真）、物理仿真系统（积累经验）、AutoDL 等云平台
3. **聚焦小问题，做深做透**：一个小而精的改进比大而全的系统更有价值

### 核心原则更新

> **从"读论文"到"做研究"的关键是产出，而不是更多的输入。**
>
> 物理仿真经验和 RL 理论基础已经足够，现在需要的是实践和产出。

> **成为技术专家的路径是深度，而不是广度。**
>
> 在"物理感知的机器人控制"这个方向上建立深度，比在多个方向上浅尝辄止更有价值。

> **稀缺性来自跨界能力。**
>
> 物理仿真 × 具身智能的组合非常稀缺，这是核心竞争力。

---

## 九、实验记录方法（2026.07.01 补充）

### 核心原则：记因果，不记流水账

实验记录的目的不是存档，而是帮助理解因果关系。四个要素就够：

```markdown
## 实验 N：简短标题

**假设**：我认为 X 会导致 Y（因为...）

**改动**：只写和 baseline 不同的部分
- reward: 加了 XX 惩罚，权重 0.1
- 其他参数不变

**结果**：
- 训练到 N 万步时，行走距离从 A 变成 B
- （截图 / 关键数字即可，不需要完整曲线）

**结论**：
- 符合预期 / 不符合预期
- 我的解释是...
- 下一步可以试...
```

### 五条具体建议

**1. 先写假设再动手**

哪怕假设很朴素（"我觉得加这个惩罚会让步态更稳"），写下来能帮你在看结果时判断因果。不写假设，事后很容易"怎么解释都行"。

**2. 一次只改一个东西**

这是最重要的一条。同时改 reward 权重和网络结构，结果好了也不知道为什么。

**3. 负面结果同样值得记**

"加了 XX 惩罚反而更差"比"加了 XX 惩罚效果更好"学到的东西可能更多。它帮你排除一个方向、理解一个机制。

**4. 不需要完美的图和数据**

截图 + 几个关键数字就行。物理仿真笔记里那种"问题→排查→定位→解决"的写法同样适用于 RL 实验：

```markdown
## 问题：机器人走了几步就倒

**排查**：
- 看录像发现是侧向晃动越来越大
- 检查 reward，发现没有侧向速度惩罚

**改动**：加了 lateral_velocity 惩罚，权重 0.5

**结果**：能走 20 步不倒了，但速度明显变慢

**结论**：侧向稳定性确实受这个 reward 影响。但权重可能太大，
下一步试试 0.2。
```

**5. 建一个索引**

随着实验增多，在文件开头维护一个简单的表格：

```markdown
| # | 改动 | 结果 | 结论 |
|---|------|------|------|
| 1 | baseline | 走 5 步倒 | 起点 |
| 2 | +侧向惩罚 0.5 | 走 20 步，变慢 | 有效但权重过大 |
| 3 | +侧向惩罚 0.2 | ... | ... |
```

这样回头看一目了然，也能发现趋势。

### 不需要做的

- ❌ 抄论文方法的完整介绍（已经在论文笔记里了）
- ❌ 罗列所有超参数（只写改了的）
- ❌ 画精美的图表（截图够用）
- ❌ 等"有重大发现"才开始记（每一步都记）

### 与阶段 1 的衔接：三条实验路线

当前阶段 1 的三个子任务：

1. ✅ 跑通基础 RL 控制任务（人形机器人行走）
2. ⬜ 尝试不同的 RL 算法（PPO 等）
3. ⬜ 理解奖励设计（参考 DeepMimic, AMP 等方法）

"跑通"已经完成，接下来有三条路线可以走，都能帮你从"跑通"过渡到"理解"。建议都试试，顺序不限：

#### 路线 A：改 reward

**目标**：理解 reward 设计如何影响行为

reward 是 RL 控制的灵魂——DeepMimic、AMP、PhysDiff 等方法的核心差异都在 reward 设计上。改一个 reward term 就能看到行为变化，是最直观的因果实验。

**具体做法**：
- 分析当前行走任务的 reward 由哪些 term 组成
- 选一个 term，调整权重（放大或缩小），观察行为变化
- 或者加一个新的 term（比如侧向速度惩罚、关节力矩惩罚），观察是否出现预期行为
- 记录：假设→改动→结果→结论

**能学到什么**：reward shaping 的直觉——什么样的 reward 引导什么样的行为，这是后面做物理感知控制的核心能力。

#### 路线 B：改 observation / action space

**目标**：理解仿真器里什么信息在驱动决策

机器人的 observation 包含了它能"看到"的所有信息（关节角度、速度、质心位置等），action space 决定了它如何输出控制信号（关节力矩 vs 目标位置）。改变这些，能帮你理解"信息"和"控制"的关系。

**具体做法**：
- 去掉某个 observation（比如去掉质心速度），看训练是否变慢或效果变差
- 增加一个 observation（比如加入前方地形信息），看是否改善适应性
- 改 action space 的表示方式（如果支持的话）
- 记录：假设→改动→结果→结论

**能学到什么**：理解 observation 的信息量如何影响策略学习，这对后续设计复杂任务的控制策略至关重要。

#### 路线 C：调超参 / 换算法

**目标**：建立对训练稳定性的工程直觉

rsl_rl 默认用 PPO，但 PPO 本身有多个超参数（learning rate、clip ratio、GAE lambda、entropy coefficient 等）。不同的超参组合会导致训练收敛速度、稳定性、最终性能的显著差异。

**具体做法**：
- 调整 learning rate（放大 2 倍或缩小 2 倍），观察 loss 曲线变化
- 调整 clip ratio（PPO 的核心参数），理解它如何限制策略更新幅度
- 尝试调整 network architecture（层数、隐藏单元数）
- 记录：假设→改动→结果→结论

**能学到什么**：训练稳定性不是"理所当然"的——同样的算法，超参数不同，结果天差地别。这种工程直觉只有动手调过才能获得。

#### 怎么选

三条路线没有严格的先后顺序，可以交替进行：
- 如果你对"什么行为是好的"更感兴趣 → 先走路线 A
- 如果你对"仿真器怎么工作的"更感兴趣 → 先走路线 B
- 如果你对"训练为什么不稳定"更感兴趣 → 先走路线 C

每条路线做 1-2 个实验就够，重点是**用实验记录的格式把因果关系写下来**，而不是穷举所有可能性。兴趣不是想出来的，是做出来的——做着做着你就知道哪个方向更值得深入了。
